<div class="date-block">
    @if (Appointment != null)
    {
        var (Color, BorderColor, falksdfj) = Appointment.GetColors();
        //Question: is there a way to simplify the code and avoid repeating Appointment?. multiple times?
        <div class="appointment" info=@Appointment?.Info
            style="background-color: @Appointment?.Background; border: 2px solid @Appointment?.BorderColor; width: calc(@_width - 4px);">
            <div class="appointment-content">
                <div class="appointment-title" style="color:@Appointment?.Color"> @Appointment?.Content
                </div>
                <div class="appointment-time">
                    <span class="start-time">@Appointment?.BeginTime</span>
                    @if (Appointment?.EndTime != null)
                    {
                        <span class="end-time"> | @Appointment?.EndTime</span>
                    }
                </div>
            </div>
        </div>
    }
</div>
